import 'package:flutter/material.dart';

class LiveHistoryView extends StatelessWidget {
  const LiveHistoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
import 'package:flutter/material.dart';

class AffilateScrren extends StatelessWidget {
  const AffilateScrren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}